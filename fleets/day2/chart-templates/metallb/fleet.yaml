defaultNamespace: metallb-system

helm:
  releaseName: metallb
  chart: "oci://registry.suse.com/edge/metallb-chart"
  version: "0.14.3"

  # custom chart value overrides
  values:
    controller:
      image:
        repository: "registry.suse.com/edge/metallb-controller"
        tag: "v0.14.3"
        pullPolicy: IfNotPresent