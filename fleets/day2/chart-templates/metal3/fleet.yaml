defaultNamespace: metal3-system

helm:
  releaseName: metal3
  chart: "oci://registry.suse.com/edge/metal3-chart"
  # TODO: STILL MISSING ON THE registry.suse.com, VALIDATE BEFORE 3.0.1 PR!
  version: "0.7.1"
  # custom chart value overrides
  values: {}
