apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: rke2-upgrade
  namespace: fleet-default
spec:
  branch: main
  paths:
  - fleets/day2/rke2-upgrade
  repo: https://github.com/suse-edge/fleet-examples.git
  targets:
  # Match everything
  - clusterSelector: {}